<%=
  form_tag(
    { controller: 'wiki', action: 'search', web: @web.address },
    { id: 'navigationSearchForm', method: 'get' }
  ) do
%>
  <%= text_field_tag('query', params[:query], { id: 'searchField' }) %>
  <%= submit_tag('Go') %>
<% end %>
